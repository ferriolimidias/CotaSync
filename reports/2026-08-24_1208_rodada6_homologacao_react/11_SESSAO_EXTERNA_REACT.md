# Sessão Externa React

Rota: `/configuracoes`.

Endpoints: `/api/v1/external-session/status`, `/api/v1/external-session/open-login`, `/api/v1/external-session/validate`.

Teste: E2E navegou pela rota e o smoke HTTP validou browser status. Não foi executado login manual real, senha ou MFA.

Estado final: UI e API disponíveis; homologação de renovação real de sessão externa permanece manual.
