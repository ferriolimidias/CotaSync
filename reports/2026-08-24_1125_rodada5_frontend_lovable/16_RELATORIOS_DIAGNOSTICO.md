# Relatórios Diagnóstico

Relatórios: `/api/v1/reports/runs`, `/api/v1/reports/batches`, filtros por ação/status e paginação.

Diagnóstico: `/api/v1/diagnostics/system`, `/api/v1/worker/status`, `/api/v1/browser/status`. Área técnica/admin, sem secrets.

Contexto: Rodada 5 importou `/home/joseadmin/front.zip` para `frontend-react/`, preservando o Streamlit em `frontend/` durante a transição.

