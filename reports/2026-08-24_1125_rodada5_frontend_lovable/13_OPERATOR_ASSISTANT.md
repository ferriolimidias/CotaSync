# OperatorAssistant

Componente: `src/components/cotasync/OperatorAssistant.tsx`.

Funcionalidades: inserir, inserir + Tab, Tab, Enter, limpar campo ativo, modo sensível e marcação de variável canônica (`grupo`, `cota`, `versao`).

Texto sensível não é persistido em storage nem estado global e é limpo após inserção.

Contexto: Rodada 5 importou `/home/joseadmin/front.zip` para `frontend-react/`, preservando o Streamlit em `frontend/` durante a transição.

