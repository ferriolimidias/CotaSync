# Estrutura Frontend Importado

Destino: `frontend-react/`.

Estrutura final: `package.json`, `bun.lock`, `src/`, `components.json`, `vite.config.ts`, `Dockerfile`, `.dockerignore`, `serve.mjs`.

Sanitização: removidos `.lovable/project.json`, `src/lib/mock-data.ts` e `src/components/cotasync/ActionCard.tsx` após o commit de marco original.

Artefatos não versionados: `node_modules`, `.output`, `.wrangler`, `.tanstack/tmp`.

Contexto: Rodada 5 importou `/home/joseadmin/front.zip` para `frontend-react/`, preservando o Streamlit em `frontend/` durante a transição.

