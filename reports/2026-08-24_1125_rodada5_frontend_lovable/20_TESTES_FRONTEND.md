# Testes Frontend

Comandos em container Bun: `bun run typecheck`, `bun run lint`, `bun run build`.

Resultado: typecheck OK, lint 0 erros/7 warnings herdados de Fast Refresh em shadcn/auth, build OK.

Contexto: Rodada 5 importou `/home/joseadmin/front.zip` para `frontend-react/`, preservando o Streamlit em `frontend/` durante a transição.

