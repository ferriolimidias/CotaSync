# Ensinar Ação Browser Workspace

Componentes: `BrowserWorkspace`, rota `/ensinar-acao`.

API: `/api/v1/learning/*`, `/api/v1/browser/status`, `/api/v1/browser/view-token`, `/api/v1/browser/ensure-ready`.

Fluxo: criar sessão, iniciar gravação, operar no navegador, finalizar ensino e publicar. Detalhes técnicos ficam ocultos.

Contexto: Rodada 5 importou `/home/joseadmin/front.zip` para `frontend-react/`, preservando o Streamlit em `frontend/` durante a transição.

