# Execução Individual

Backend adicionado: `POST /api/v1/actions/{action_id}/run`.

Status visual preparado em ações/execução. A execução individual usa fachada v1 e não depende mais do endpoint legado `/api/actions/{id}/run`.

Contexto: Rodada 5 importou `/home/joseadmin/front.zip` para `frontend-react/`, preservando o Streamlit em `frontend/` durante a transição.

