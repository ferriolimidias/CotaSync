# Clientes

API: `/api/v1/clients`.

Funcional: listar, buscar, filtrar, criar, editar e desativar. Campos: nome, lista/grupo, grupo, cota, versão, ativo, observações e variáveis canônicas.

Pendente: importação CSV com preview/validação/conflitos em fachada v1. Botões ficaram desabilitados.

Contexto: Rodada 5 importou `/home/joseadmin/front.zip` para `frontend-react/`, preservando o Streamlit em `frontend/` durante a transição.

