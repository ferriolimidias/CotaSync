# Camada API React

Arquivo: `frontend-react/src/services/api.ts`.

Implementa `VITE_API_BASE_URL`, credentials/cookies, CSRF centralizado, timeout, mensagens amigáveis, paginação, batches com `Idempotency-Key`, browser/noVNC, learning, diagnostics e reports.

Staging usa mesma origem `http://127.0.0.1:3300/api` com proxy em `serve.mjs`.

Contexto: Rodada 5 importou `/home/joseadmin/front.zip` para `frontend-react/`, preservando o Streamlit em `frontend/` durante a transição.

