# Dashboard

API: `/api/v1/dashboard` e `/api/v1/reports/runs`.

Mostra sessão externa, clientes ativos, ações prontas, execuções do dia, última execução, sistema de execução, fila e alertas. Não expõe detalhes de CDP/PostgreSQL na tela principal.

Contexto: Rodada 5 importou `/home/joseadmin/front.zip` para `frontend-react/`, preservando o Streamlit em `frontend/` durante a transição.

