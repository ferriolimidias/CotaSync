# Config Sessão Externa

API: `/api/v1/external-session/status`, `/open-login`, `/validate` e browser v1.

A UI permite abrir sessão para login manual e validar configuração. Não automatiza senha nem MFA.

Contexto: Rodada 5 importou `/home/joseadmin/front.zip` para `frontend-react/`, preservando o Streamlit em `frontend/` durante a transição.

